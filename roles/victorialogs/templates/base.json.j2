{
  "_comment": "{{ ansible_managed }} - the half of the store's users that Ansible owns. The writer owns users.d/.",
  "backend": "{{ victorialogs_vmauth_backend }}",
  "operator_token": "{{ victorialogs_vmauth_operator_read_token }}",
  "bridge_token": "{{ victorialogs_bridge_token }}"
}
